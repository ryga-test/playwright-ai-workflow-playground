# Playwright AI Workflow Playground environment template
# Copy this file to `.env` and adjust values for your local machine.
# Do not commit `.env` or real credentials, cookies, tokens, storage state, or customer data.

EXAMPLE_BASE_URL=http://localhost:3000
